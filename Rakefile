# frozen_string_literal: true

require_relative "config/application"

# --- PARCHE CRÍTICO HACKATHON RUBY 4.0 ---
require "ostruct"
require "matrix"
# -----------------------------------------

Rails.application.load_tasks